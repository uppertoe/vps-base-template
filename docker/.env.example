# Copy this to .env in your server repo (NOT this scaffold repo)
# Never commit .env — it contains secrets

# Your server's primary domain
DOMAIN=myserver.example.com

# Set to true for local development to use self-signed certs
LOCAL=false
